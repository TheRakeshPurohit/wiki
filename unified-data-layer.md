# 统一数据层架构：探索走 PG，规模化走 KV + Iceberg + DuckDB

**结论**：数据栈不是一套通用的东西，而是按**项目属性**分成两条路径——**探索 / 敏捷型**直接无脑用 PostgreSQL；**规模化 / 可预测型**收敛到 **KV + Iceberg + DuckDB**。分叉的判断轴是「需求可预测性 × 时间尺度」，负载分层（OLTP/OLAP）只是路径 B 内部的组织方式，不是全局唯一答案。

## 判据：两条路径的分叉轴

统一数据层不存在「唯一正确栈」，只存在「适合这个项目此刻状态的栈」。分叉由两个正交维度决定：

- **需求可预测性**：你掌控需求、查询在设计期可固化 → 值得押注 KV；需求随外部变化、不可预测 → 不值得提前建键空间。
- **时间尺度 / 规模化**：探索验证、生命周期未定 → 用心智负担最低的普适方案；长期演进、要规模化并彻底剔除中间商 → 收敛到物理极限的极简栈。

两维都指向「暂时依附」→ **路径 A（PG）**；都指向「长期规模化」→ **路径 B（KV + Iceberg + DuckDB）**。中间地带（需求已可预测、但尚无规模化诉求）也归路径 B——KV 的零运维让它在没有规模化场景时依然成立。

## 路径 A：探索 / 敏捷——无脑 PostgreSQL

当代数据工程的信条：**如果你不知道用什么数据库，无脑用 Postgres 就对了。**

PG 的泛化能力使它成为探索期的最优默认：

- **关系 + JSONB**：结构化与半结构化数据一引擎接住，schema 灵活到接近 NoSQL。
- **扩展生态**：图（Apache AGE）、向量（pgvector）、全文检索、时空——一个进程覆盖绝大多数「别让我现在决定」的负载。
- **心智负担最低**：生态最全、运维最熟、人才最多，现成工具全部直接对接。

**适用对象**：需求不确定的 MVP、快速验证、单团队敏捷迭代、生命周期未定的实验项目。此时为存储选型过早下注是负收益——PG 用一个引擎把你的选择延后到需求清晰之后再谈。

**升级信号（何时迁往路径 B）**：当需求固化到可预测、数据规模开始增长、出现长期归档与分析诉求时，PG 的关系型解析器运行时成为不必要的中间层。迁移 = 沿键空间重新建模：先枚举已固化的访问模式，再反推复合键布局（见 [KV 存储引擎](kv-storage-engine.md) §访问模式驱动）。

## 路径 B：规模化 / 可预测——KV + Iceberg + DuckDB

长期演进、追求极简的团队把数据栈收敛到两件套：嵌入式 KV 管可变状态，Iceberg 管历史与真相，DuckDB 管分析查询。**路径 B 没有 PG**——KV 不只与 PG 并列，而是取代 PG 的 OLTP 角色。

```
┌───────── OLTP 可变状态 ──────────┬───────── OLAP 静态分析 ──────────┐
│ 嵌入式 KV（Fjall / SlateDB + S3）│ DuckDB（查询层）                 │
│ 可预测查询模式 / 大规模状态      │ 嵌入列式，向量化执行             │
│ 取代 PG 的 OLTP 角色             ├──────────────────────────────────┤
│ 进程内零 RTT · 单一二进制        │ Iceberg（存储层）                │
│                                  │ S3 为 Truth + 无状态化 Catalog   │
│                                  │ 只读所需列，高压缩，聚合下推     │
└──────────────────────────────────┴──────────────────────────────────┘
```

- **OLTP：嵌入式 KV**——查询模式可预测 / 大规模状态用 KV 取代 PG。可预测与否取决于需求可控性（话语权），而非组织身份。零解析、零优化器、进程内零 RTT、单一二进制；数据量大时 KV 的物理优势即便模式多变也成立。完整论证见 [KV 存储引擎](kv-storage-engine.md)。
- **OLAP：Iceberg + DuckDB**——历史与分析数据归 Iceberg 落在 S3（对象存储为唯一 Truth），DuckDB 做嵌入式查询层，只读所需列、同型值压缩、过滤聚合下推。Catalog 可无状态化：本地 KV 只是 `table → snapshot` 缓存，丢了扫 S3 重建。详见 [Lakehouse 研究](lakehouse-research.md)。
- **持久与分布**：Lakehouse 同时承担 OLTP 的**持久备份**与对外 **OLAP 服务**——异步 Flush 把 Fjall 状态落入 Iceberg，DuckDB 及第三方引擎经标准表格式读取同一份 S3 数据。由此「Fjall + Lakehouse」与「SlateDB + S3」是两条并行路径：前者本地写入、落湖为备份，后者直接以 S3 为存储后端。共识层（Openraft）只承载**元数据规模**协调（登录态、Catalog 指针、配置）——Raft 不分片，每节点为全量副本，存储与写放大随节点数线性增长（3x 可接受，但无法随节点数扩展），不适合大规模同步数据。
- **收敛哲学**：剔除中间商——Redis 的网络态缓存、关系型的解析器运行时、独立数仓的运维，都由这两件套吸收。服务即数据库，数据库即服务。

## 为什么路径 B 也不选单引擎（SurrealDB）

路径 B 收敛进一个多模型单引擎（如 SurrealDB）是**已评估、未采用**的路线。马太效应决定挑战者必须在足够多维度形成压倒性优势才能克服生态惯性——SurrealDB 在语言设计、多态关联、计算下推上碾压，但在生态、工具链、云支持、数据一致性上全面劣势，**互有胜负 = 挑战失败**。且两件套在任意单引擎都达不到的尺度上拆分 OLTP 与 OLAP：图/多模型需求被 KV 用多模型编码吸收，分析负载归 DuckDB·Lakehouse。完整评估见 [SurrealDB 评估档案](query-language-design.md)。

## OLTP 与 OLAP 的边界

- **OLTP（可变状态）**：路径 A 由 PG 承担，路径 B 由嵌入式 KV 承担（查询模式可预测、大规模状态）。这一层负责实时读写。
- **OLAP（静态分析）**：DuckDB + Iceberg（路径 B）或路径 A 的临时分析。通过 CDC / 批量 Flush 将历史数据流入湖中，可变状态与静态分析分离，详见 [Lakehouse 研究](lakehouse-research.md) 与 [KV 存储引擎](kv-storage-engine.md)。

## 存算分离与分层位置

- **PG（路径 A）**：单体扩展（插件/JSONB），存算分离由云厂商提供。
- **KV（路径 B）**：嵌入式（Fjall + Openraft）或 S3 后端（SlateDB），进程内零 RTT 或对象存储持久化。
- **DuckDB（路径 B）**：嵌入列式，存算分离由 Lakehouse 提供。
- **Iceberg（路径 B）**：S3 + Catalog，存储与计算彻底解耦。

## 结论

没有「一个引擎做所有事」，也没有「一套栈适合所有项目」。**探索 / 敏捷走 PG 无脑开局，规模化 / 可预测收敛到 KV + Iceberg + DuckDB**。分叉轴是需求可预测性与时间尺度；负载分层是路径 B 内部的组织方式。SurrealDB 的单引擎统一路径经评估未采用——其查询语言洞见保留在评估档案中，但核心架构不依赖它。

## 交叉引用

- **[KV 存储引擎](kv-storage-engine.md)**：嵌入式 KV 与分布式共识（Fjall + Openraft）设计；可预测查询模式与大规模场景下 KV 替代 PG 的论证。
- **[Lakehouse 研究](lakehouse-research.md)**：分析层选型——Delta/Iceberg/Lance、对象存储、落地模式、Catalog 无状态化。
- **[SurrealDB 评估档案](query-language-design.md)**：SurQL 语言哲学、图建模、挑战者逻辑的完整评估（为何未进核心）。
- **[Redis 批判](redis-critique.md)**：网络 RTT 陷阱与缓存分层。
- **[Aura 架构 §5](aura-architecture.md)**：需要完全嵌入式 KV 与共识时的轻量方案。