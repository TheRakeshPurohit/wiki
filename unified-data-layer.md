# 统一数据层架构：探索走 PostgreSQL，规模化走 KV + Iceberg + DuckDB

**结论**：数据栈不是一套通用的东西，而是按**项目属性**分成两条路径——**探索 / 敏捷型**直接无脑用 PostgreSQL；**规模化 / 可预测型**收敛到 **KV + Iceberg + DuckDB**。分叉的判断轴是「需求可预测性 × 时间尺度 × 负载重量（规模）」，负载分层（OLTP/OLAP）只是路径 B 内部的组织方式，不是全局唯一答案。

## 判据：两条路径的分叉轴

统一数据层不存在「唯一正确栈」，只存在「适合这个项目此刻状态的栈」。分叉由三个正交维度决定：

- **需求可预测性**：你掌控需求、查询在设计期可固化 → 值得押注 KV；需求随外部变化、不可预测 → 不值得提前建键空间。
- **时间尺度 / 生命周期**：探索验证、生命周期未定 → 用心智负担最低的普适方案；长期演进、要彻底剔除中间商 → 收敛到物理极限的极简栈。
- **负载重量 / 规模**：负载轻、不必多实例，可共用集中式存储池（池化红利）；负载重、数据量大到共享池成瓶颈、独占实例也到顶时，**重负载最终只能选 KV**（进程内零 RTT、零解析、单实例扛量，量再涨有平滑扩展出口）。

两维都指向「暂时依附」→ **路径 A（PostgreSQL）**；都指向「长期规模化」→ **路径 B（KV + Iceberg + DuckDB）**。中间地带（需求已可预测、但尚无规模化诉求）也归路径 B——KV 的零运维让它在没有规模化场景时依然成立。负载维度与其余两维同向：负载轻，集中式池化优势成立、走路径 A；负载重，最终只能选路径 B 的 KV。

## 路径 A：探索 / 敏捷——无脑 PostgreSQL

当代数据工程的信条：**如果你不知道用什么数据库，无脑用 Postgres 就对了。**

PostgreSQL 的泛化能力使它成为探索期的最优默认：

- **关系 + JSONB**：结构化与半结构化数据一引擎接住，schema 灵活到接近 NoSQL。
- **扩展生态**：图（Apache AGE）、向量（pgvector）、全文检索、时空——一个进程覆盖绝大多数「别让我现在决定」的负载。
- **心智负担最低**：生态最全、运维最熟、人才最多，现成工具全部直接对接。
- **集中式服务形态（路径 A 独有，相对路径 B 的嵌入式是实打实的优势）**：PostgreSQL 以独立服务形态部署，单一实例可服务多个项目（资源池化、复用连接与缓冲），可上云托管（RDS/CloudSQL 免自运维），应用层保持无状态（存储外置为唯一持久点）。这三条红利**来自「集中式 vs 嵌入式」的部署形态，不是 SQL 语言性质**——若路径 B 想拿同样红利，正解是换集中式 KV server（Redis/etcd/FDB/TiKV），而非回到 SQL。此处把它们记为路径 A 的优势，正因为路径 A 恰好以 PostgreSQL=集中式形态落地；论断源于集中式属性，只是落在 PostgreSQL 头上（归因辨别见 [KV 存储引擎](kv-storage-engine.md)「资源池化 / 云托管 / 无状态：是拓扑性质，不是 SQL 的性质」）。

**适用对象**：需求不确定的 MVP、快速验证、单团队敏捷迭代、生命周期未定的实验项目。此时为存储选型过早下注是负收益——PostgreSQL 用一个引擎把你的选择延后到需求清晰之后再谈。

**升级信号（何时迁往路径 B）**：当需求固化到可预测、数据规模开始增长、出现长期归档与分析诉求时，PostgreSQL 的关系型解析器运行时成为不必要的中间层。迁移 = 沿键空间重新建模：先枚举已固化的访问模式，再反推复合键布局（见 [KV 存储引擎](kv-storage-engine.md) §访问模式驱动）。

## 路径 B：规模化 / 可预测——KV + Iceberg + DuckDB

长期演进、追求极简的团队把数据栈收敛到两件套：嵌入式 KV 管可变状态，Iceberg 管历史与真相，DuckDB 管分析查询。**路径 B 没有 PostgreSQL**——KV 不只与 PostgreSQL 并列，而是取代 PostgreSQL 的 OLTP 角色。

```
┌───────── OLTP 可变状态 ──────────┬───────── OLAP 静态分析 ──────────┐
│ 嵌入式 KV（Fjall / SlateDB + S3）│ DuckDB（查询层）                 │
│ 可预测查询模式 / 大规模状态      │ 嵌入列式，向量化执行             │
│ 取代 PostgreSQL 的 OLTP 角色     ├──────────────────────────────────┤
│ 进程内零 RTT · 单一二进制        │ Iceberg（存储层）                │
│                                  │ S3 为 Truth + 无状态化 Catalog   │
│                                  │ 只读所需列，高压缩，聚合下推     │
└──────────────────────────────────┴──────────────────────────────────┘
```

- **OLTP：嵌入式 KV**——查询模式可预测 / 大规模状态用 KV 取代 PostgreSQL。可预测与否取决于需求可控性（话语权），而非组织身份。零解析、零优化器、进程内零 RTT、单一二进制；数据量大时 KV 的物理优势即便模式多变也成立。完整论证见 [KV 存储引擎](kv-storage-engine.md)。
- **OLAP：Iceberg + DuckDB**——历史与分析数据归 Iceberg 落在 S3（对象存储为唯一 Truth），DuckDB 做嵌入式查询层，只读所需列、同型值压缩、过滤聚合下推。Catalog 可无状态化：本地 KV 只是 `table → snapshot` 缓存，丢了扫 S3 重建。详见 [Lakehouse 研究](lakehouse-research.md)。
- **持久与分布**：Lakehouse 同时承担 OLTP 的**持久备份**与对外 **OLAP 服务**——异步 Flush 把 Fjall 状态落入 Iceberg，DuckDB 及第三方引擎经标准表格式读取同一份 S3 数据。分布形态在「Fjall + Lakehouse」与「SlateDB + S3」间选择，见下节《两条分布路径》。
- **收敛哲学**：剔除中间商——Redis 的网络态缓存、关系型的解析器运行时、独立数仓的运维，都由这两件套吸收。服务即数据库，数据库即服务。

### 两条分布路径：Fjall + Lakehouse vs SlateDB + S3

分水岭先看**时间局部性**：若热数据是近期写入、能被 memtable / 本地缓存兜住（Actor 消息、会话状态这类），冷读概率低——SlateDB 的 S3 往返只在偶发点查触发，可接受，甚至可**取代「Fjall + Lakehouse」两件套**（单真相、免 flush 管线）。仅当热读取对进程内 ns-μs 有硬要求、且读取无时间局部性时，Fjall 才是必需。

| 维度 | Fjall + Iceberg | SlateDB + S3 |
|:---|:---|:---|
| 真相位置 | 本地磁盘（serving）+ Iceberg 落湖，两处 | S3 即真相，一处 |
| 热路径服务延迟 | 进程内零 RTT（ns-μs） | 缓存命中快；冷读走 S3（ms） |
| 持久性 / 容灾 | 单节点本地；落湖滞后=崩溃丢失窗口 | S3 3-AZ 原生，无独立备份管线 |
| 跨实例共享 / 读扩展 | 单节点进程内；共享需经 Iceberg/共识 | 多实例读共享 S3，读天然水平扩展 |
| 写入路径 | 本地 WAL→SSTable（极快） | memtable→异步 flush S3 |
| 架构件数 | 两套 + 自建 flush 管线 | 一套 |
| 一致性 | KV 与 Iceberg 间 eventual（flush 滞后） | 单真相，manifest 协调 |
| 分析 | DuckDB 读 Iceberg（标准表格式/时旅/演进） | DataFusion 直接查；接 DuckDB/BI 需导出 Parquet/Iceberg |
| 导向规模 | 单机中规模 + 归档分析分离 | 大规模、多写多读、存算分离 |
| 成熟度 | Fjall 成熟；管线自搭 | SlateDB 相对新，生产待验证 |

**Fjall + Iceberg 赢在「热」**：进程内直接 `scan` 服务请求、确定性响应，Iceberg 兜持久与分析；代价是两套真相 + flush 滞后（节点落湖前崩溃丢最近写入），且热数据无法靠共识复制来跨实例共享——Raft/Openraft 不分片、每节点全量副本，存储与写放大随节点数线性增长，只配管元数据规模。

**SlateDB 赢在「单真相 + 扩展」**：S3 唯一真、无独立备份管线、多实例读共享，收齐存算分离红利（副本固定 3-AZ，与节点数无关）；代价是冷读的 S3 往返，热路径靠内存/本地缓存兜底，点查不及 Fjall 的嵌入式零 RTT。

**决策**：有强时间局部性、或接受偶发冷读 → SlateDB + S3（单真相、可扩展）；对进程内 ns-μs 有硬要求且读取无时间局部性 → Fjall + Iceberg。二者是优化目标不同（单真相 + 扩展 vs 进程内热延迟）的两条路，非同类替换。

**强一致分布式不自建**：若需要的是强一致的数据复制 + 水平扩展（「分片 + Raft」形态），别把嵌入式引擎拼成分片 + Raft（那等于重造分布式数据库），直接用成熟的 FoundationDB / TiKV——要跨片 ACID 用 FDB，分片 KV + 片内一致用 TiKV。对比详见 [KV 存储引擎](kv-storage-engine.md) §强一致分布式。

## 为什么路径 B 也不选单引擎（SurrealDB）

路径 B 收敛进一个多模型单引擎（如 SurrealDB）是**已评估、未采用**的路线。马太效应决定挑战者必须在足够多维度形成压倒性优势才能克服生态惯性——SurrealDB 在语言设计、多态关联、计算下推上碾压，但在生态、工具链、云支持、数据一致性上全面劣势，**互有胜负 = 挑战失败**。且两件套在任意单引擎都达不到的尺度上拆分 OLTP 与 OLAP：图/多模型需求被 KV 用多模型编码吸收，分析负载归 DuckDB·Lakehouse。完整评估见 [SurrealDB 评估档案](query-language-design.md)。

## OLTP 与 OLAP 的边界

- **OLTP（可变状态）**：路径 A 由 PostgreSQL 承担，路径 B 由嵌入式 KV 承担（查询模式可预测、大规模状态）。这一层负责实时读写。
- **OLAP（静态分析）**：DuckDB + Iceberg（路径 B）或路径 A 的临时分析。通过 CDC / 批量 Flush 将历史数据流入湖中，可变状态与静态分析分离，详见 [Lakehouse 研究](lakehouse-research.md) 与 [KV 存储引擎](kv-storage-engine.md)。

## 存算分离与分层位置

- **PostgreSQL（路径 A）**：单体扩展（插件/JSONB），存算分离由云厂商提供。
- **KV（路径 B）**：嵌入式（Fjall + Openraft）或 S3 后端（SlateDB），进程内零 RTT 或对象存储持久化。
- **DuckDB（路径 B）**：嵌入列式，存算分离由 Lakehouse 提供。
- **Iceberg（路径 B）**：S3 + Catalog，存储与计算彻底解耦。

## 结论

没有「一个引擎做所有事」，也没有「一套栈适合所有项目」。**探索 / 敏捷走 PostgreSQL 无脑开局，规模化 / 可预测收敛到 KV + Iceberg + DuckDB**。分叉轴是需求可预测性、时间尺度与负载重量（规模）；负载分层是路径 B 内部的组织方式。SurrealDB 的单引擎统一路径经评估未采用——其查询语言洞见保留在评估档案中，但核心架构不依赖它。

## 交叉引用

- **[KV 存储引擎](kv-storage-engine.md)**：嵌入式 KV 与分布式共识（Fjall + Openraft）设计；可预测查询模式与大规模场景下 KV 替代 PostgreSQL 的论证。
- **[Lakehouse 研究](lakehouse-research.md)**：分析层选型——Delta/Iceberg/Lance、对象存储、落地模式、Catalog 无状态化。
- **[SurrealDB 评估档案](query-language-design.md)**：SurQL 语言哲学、图建模、挑战者逻辑的完整评估（为何未进核心）。
- **[Redis 批判](redis-critique.md)**：网络 RTT 陷阱与缓存分层。
- **[Aura 架构 §5](aura-architecture.md)**：需要完全嵌入式 KV 与共识时的轻量方案。