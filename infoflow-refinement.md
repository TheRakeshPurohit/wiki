# 信息流精炼：从浏览器入口的信息管道

浏览器是多数人信息的主要流入途径。**信息流精炼**把"看网页"这个被动动作，变成一条主动的处理管道：**捕获 → 属性化 → 精炼 → 消费**。它不绑定某个工具——`mudra`（浏览器会话）是第一个落地底座，方向本身是通用的，也是某个更大系统（监督 / 态势感知）的**输入侧**。

内容组织轴见 `tag-forest.md`（标签森林）；本文是**处理管道**设计。两者正交：标签森林管"一个页面属于哪里、多重要"，信息流管道管"信息如何流入、被加工、被消费"。

## 分层

| 层 | 做什么 | 主要手段 |
|---|---|---|
| **捕获** | 信息进管道 | 浏览器（页面流入）、RSS/订阅监控、页内链接挖掘 |
| **属性化** | 打标 + 评分 | 规则 + ML：situation/topic 标签、importance/urgency 评分 |
| **精炼** | 提纯、按价值筛选排序 | 转 Markdown、聚合摘要、全文检索 |
| **消费** | 产出 | 跳转 / 存档 / 周报 / LLM 总结 / 态势感知 |

## 属性化：NB 评分（importance / urgency）

**问题定性**：两者都是 5 级有序分类（`☆..☆☆☆☆☆`）、训练数据少（手动评分几十~几百条）、强特征=域名/子域名、要求可解释、底座为 stdlib-only。

**方案：朴素贝叶斯（手写，纯 stdlib）**——小数据高维稀疏特征下 NB 稳；每个 `token → P(等级)` 完全可解释（可当规则建议）；约 15 行可实现、零新依赖；urgency 弱数据时比判别式健壮。不用深层模型（数据不够）也不用 LLM（重/慢/黑箱/不可审）。

**利用有序性**：5 级不是无序类别。算 `P(class k | features)` 后取**加权期望** `score = Σ k·P_k`（0~5 连续）再 snap 到 `☆`，避免相邻级互抢。

**渐进落地（规则先行 + ML 建议规则，非黑箱替代）**：
1. 规则底座：域名/子域名继承表 → 默认分 + 手动覆盖（infoq/oschina/csdn 即此）。
2. NB 学习：从手动评分学 token 贡献 → 对未见域名给默认分、高置信 token 列成"建议规则"供审。
3. 人工落表：采纳进规则表 → 可解释、可控。

**特征**（importance / urgency 分开）：
- importance：**域名层级**（`infoq.com` vs `content.infoq.com`）+ URL token
- urgency：时效/新闻性，靠 **title 词频 + 时效词**（发布/更新/突发）→ 文本特征主导
- 两者都加入**正文特征**（见下）——不能只看标题。

## 正文特征与标题党检测

标题可被操纵（标题党），正文才反映真实内容。因此**评分必须结合正文，且检测标题-正文一致性**。

**正文获取**：HTML → Markdown 需先经提取工具（候选取名实现时核实：`trafilatura`、`readability-lxml`、`html2text`）。正文提取在扩展/消费层（可引轻依赖，不受 core 的 stdlib-only 约束）。转成 Markdown 后：正文文本、标题、页内链接都可获利。

**标题党检测（标题-正文一致性）**：
- 算 `consistency = |标题核心词 ∩ 正文前两段词| / |标题核心词数|`（小写、去停用词、词形归一）。
- 低一致性（如 `<30%`）→ 标记 clickbait、**下调 importance**（或作为惩罚项）。
- 标题党特征词（震惊 / 绝对 / 想不到 / 99%…）作额外特征；`consistency` 作为派生数值特征喂给 NB。
- 目的：NB 不只信标题——标题高分但正文空洞/无关的页（标题党）被压低。

## 链接挖掘（捕获延伸）

`html → md` 后能提取页内链接（`[text](url)`）及其锚文本/目标标题。用于**从被引页面反推有价值页**：被高价值页面引用的链接（尤其域名已知低噪音的）可进 `inbox` 或给建议评分，形成"页面→页面"的发现链。信息来源从"你主动访问"扩到"有价值页替你筛了一轮"。

## RSS / 订阅监控（捕获强化）

订阅源 → 新条目直接进 `inbox`，幂等去重。让"值得关注的开源/社区/博客"自动流入，成为信息进口的常设通道。

## 聚合摘要 / 态势感知（消费层，并入更大系统）

按 situation/importance 把一段时间的流入**聚合**成"关键信息态势"（今日重点、按维度的要点、被引高频）。这是**态势感知（situational awareness）**的输入侧——信息流精炼可并入更大的监督/监控系统，作为其信息入口模块，而非独立的孤立工具。

## LLM 总结 / 转 Markdown（精炼层扩展）

- **LLM 总结**：单页摘要、聚合摘要、跨页综合。只对选定的高价值页触发（成本可控），不做全量推断。
- **转 MD / 存档**：reader-mode 提取正文 → Markdown 存 wiki/剪藏；既是消费产物，也是 LLM 总结的喂料。

## 与 tag-forest / mudra 的关系

- `tag-forest.md` 定义内容组织轴；`importance/urgency` 本就是其评分树。本文定义**处理管道**——信息如何流入、被加工。
- `mudra` 是其第一个落地底座：捕获（浏览器）、属性化（打标/评分）、精炼（列宽/检索）、消费。扩展路线见 mudra `PLAN.md`。