# 长连接工程挑战：存算一体架构下的物理限制与解法

**状态**：初稿
**日期**：2026-07-29

## 导语

WebSocket / WebTransport 长连接是[反应式架构](reactive-architecture.md)动态层的核心通道，也是 [Aura](aura-architecture.md) 存算一体架构中 Actor 的物理载体。短生命周期 HTTP 请求规避了连接状态管理的复杂性，但长连接将连接生命周期与计算单元绑定后，引入了一系列物理层面的工程挑战。本文梳理这些挑战的本质与现代解法。

## 1. 背压（Backpressure）

### 问题

生产者（流计算引擎 / Actor 事件处理）的产出速度超过消费者（客户端连接）的消费速度时，数据在网关层堆积。

传统反应式框架的背压模型假设逻辑通道是单向管道——消费端变慢时，背压信号逆流传导，减缓上游发送。但在实际网络栈中，数据经过多层缓冲：

```
应用层队列 → Tokio Channel → Tokio 写缓冲 → 内核 TCP Send Buffer → 网络
```

如果网关只感知应用层队列的满/空状态，忽略了内核层 Send Buffer 的堆积，会导致两个后果：
- **内存膨胀**：应用层持续向 Channel 写入，Channel 无界增长，最终 OOM
- **延迟毛刺**：Send Buffer 堆积到 TCP 窗口缩小时，客户端感知的延迟突然跳升

### 解法

网络感知型背压（Network-Aware Backpressure）：

- **Rust/Tokio 路径**：`tokio::io::AsyncWrite::poll_write` 返回 `Poll::Pending` 时，Tokio 运行时自动暂停该连接的写入任务，上游 Channel 自然反压。关键是确保 Channel 有界（`tokio::sync::mpsc::bounded`），而非无界。
- **应用层降级**：当 Channel 积压超过阈值时，跳过低优先级事件（如心跳、状态同步），只保留业务关键事件。这是"有损背压"——丢弃非关键数据以保护系统稳定性。
- **WebTransport 优势**：WT 基于 QUIC，内置流控（Flow Control）。每个 Sub-Stream 有独立的流量窗口，背压信号在传输层自动传导，不需要应用层手动实现。这是 WT 相比 WS 的结构性优势。

## 2. 内存碎片化

### 问题

常驻长连接的典型生命周期：事件到达 → 状态突变 → 产生新数据 → 写入本地存储（Fjall）。这个循环在高并发下持续运行，导致两个内存问题：

- **堆碎片化**：频繁的 alloc/dealloc（事件对象、序列化缓冲区）导致堆内存碎片化，RSS 持续增长但实际可用内存不足
- **GC 停顿**：有 GC 的语言（Go、Java、Python）在碎片化严重时触发 Full GC，造成长停顿

### 解法

- **无 GC 语言**：Rust 的所有权系统在编译期管理内存生命周期，不存在运行时 GC 停顿。Fjall 作为 Rust 原生存储引擎，内存管理与 Actor 运行时统一。
- **Slab 分配器**：对固定大小的对象（如事件消息、连接状态）使用 Slab 分配器，避免通用堆分配器的碎片化问题。Tokio 的 `BytesMut` 和 `bytes::Bytes` 内部使用引用计数 + 零拷贝切片，减少不必要的分配。
- **堆外内存（Off-heap）**：极端场景下（百万级连接），将连接元数据和状态数据分配在堆外（`std::alloc::alloc` 或 `mmap`），绕过 GC 和堆碎片化。代价是手动管理生命周期。

## 3. 连接倾斜（Connection Skew）

### 问题

HTTP 请求是无状态的，负载均衡器可以将任何请求分发到任何节点。但 WS/WT 长连接是有状态的——一旦建立，客户端与特定节点绑定。

流量暴增时自动扩容，新节点无法分担已有连接。如果采用断开重连策略，大量客户端同时断开重连会产生惊群效应（Thundering Herd），击穿新节点。

### 解法

- **连接迁移**：WebTransport 基于 QUIC，支持 Connection Migration——客户端 IP 变化时（如 Wi-Fi 切 4G），连接自动迁移到新路径，不需要断开重连。这缓解了网络切换场景下的连接倾斜。
- **优雅迁移层**：在网关前部署一层连接代理（如 Envoy 或自定义 Rust 代理），维护"连接 → 后端节点"的映射表。扩容时，代理将新连接路由到新节点，已有连接保持不变。存量连接在空闲或低峰期自然衰减。
- **分区策略**：按用户 ID 哈希分配连接节点，确保同一用户的连接集中在一个节点。扩容时只影响哈希环上的一个区间，迁移范围可控。

## 4. 多路复用拓扑（WebTransport Sub-Streams）

### 问题

WS 是逻辑单通道——一个连接上所有消息共享同一通道。反应式框架的 Publisher/Subscriber 抽象基于这个假设。

WebTransport 支持多个独立的 Sub-Stream，每个 Sub-Stream 有独立的流控和有序性保证。这引入了"物理单连接、逻辑多通道"的二层拓扑：

```
WT 连接（物理层）
├── Sub-Stream 1: 群组 A 消息
├── Sub-Stream 2: 群组 B 消息
├── Sub-Stream 3: 全局广播
└── Sub-Stream 4: 控制指令
```

传统反应式框架的 Topic/Channel 抽象无法直接映射到这个拓扑——它们假设一个 Topic 对应一个独立的连接或队列，而不是一个连接内的子流。

### 解法

- **二层路由**：网关维护"Topic → Sub-Stream ID"的映射表。消息到达时，先查映射表确定目标 Sub-Stream，再通过 WT API 发送到对应子流。
- **细粒度权限控制**：Sub-Stream 级别的流控允许对单个群组实施独立的速率限制或暂停，而不需要断开整个连接。
- **向后兼容**：WS 客户端仍然走逻辑单通道，WT 客户端可以选择多流模式。网关层做协议适配，对上游业务层透明。

## 5. 内存拓扑：直接寻址与反向索引

存算一体架构下，连接实例、状态数据、群组关系都在同一进程的内存空间中。这消除了传统架构中"网关无状态，关系在 Redis/MySQL"的跨网络查询，但引入了内存拓扑设计问题。

### 全局控制（封禁/改密）

用户连接在边缘节点上是一个常驻的 Actor 实例，内存中持有连接句柄和状态数据。全局控制器通过 userId 直接映射到该实例：

```
全局控制器 → userId 映射表 → Actor 实例 → 连接句柄
```

封禁发生时，控制器按 userId 定位实例，释放连接（调用 OS 层 `close(fd)` 销毁 Socket），清除状态。没有中间件分发延迟。

### 群组交互：反向索引

群组管理需要两个数据结构：

- **正向索引**：群组对象持有成员列表（`Room.members = [A, B, C]`）
- **反向索引**：用户对象持有加入的群组列表（`A.joined_rooms = [Room_99, Room_42]`）

踢人操作的物理路径：

```
1. 控制器按 userId 找到用户 A 的 Actor 实例
2. 读取 A.joined_rooms（反向索引）
3. 遍历每个群组，从 Room.members 中移除 A
4. 释放 A 的连接，销毁 Actor 实例
```

群发操作：

```
1. 群组对象遍历 Room.members（正向索引）
2. 对每个存活成员的连接句柄调用推送
3. A 已不在列表中，物理上不可能收到消息
```

没有 `if(isBanned)` 判断——被踢出的用户从列表中消失，业务逻辑天然不触及已删除的实例。

### 一致性问题

上述模型假设所有操作在同一进程内完成。但当群组成员分布在多个节点时：

- 节点 1 上的 Room_99 和节点 2 上的 Room_99 是两个独立副本
- 踢人操作只修改了节点 1 的副本，节点 2 的副本仍持有 A 的连接
- A 在节点 2 上的消息仍然会被推送

这是分布式一致性的经典问题，解法取决于一致性要求：

- **最终一致性**：通过消息队列广播踢人事件，各节点异步执行。延迟窗口内 A 可能仍收到消息，但最终会被清理。
- **强一致性**：踢人操作走 Openraft 共识，所有节点确认后才释放连接。代价是每次踢人增加一次 RTT。

Aura 架构选择 Fjall + Openraft，天然支持强一致性路径——群组状态变更作为 Raft 日志条目复制到所有节点，状态机应用后各节点视图一致。

---

## 交叉引用

- **[反应式架构](reactive-architecture.md)**：长连接是动态层推送的物理通道，本文讨论该通道的工程挑战。
- **[Aura 架构](aura-architecture.md)**：存算一体引擎，本文的内存拓扑模型基于 Actor + Fjall 的进程内架构。
- **[Redis 批判](redis-critique.md)**：传统架构中连接状态和群组关系存在 Redis，本文展示存算一体如何消除这一层。
