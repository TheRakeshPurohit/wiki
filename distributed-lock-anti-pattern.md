# 分布式锁：反模式分析

## 核心论点

分布式锁在绝大多数场景是**反模式**。锁把「阻塞」这个本来就该避免的东西，放大几个数量级——它引入延迟、时钟依赖、故障窗口，却通常解决一个本可用无锁方案解决的问题。

理解分布式锁，先要剥离一个术语幻觉：**很多你以为的"分布式锁"其实不是锁**。

## 1. 术语澄清：哪些不是锁

| 场景 | 常用误解 | 真相 |
|:--|:--|:--|
| Leader 选举 | "需要分布式锁" | **lease 租约 / 所有权**，不是锁（etcd/Consul） |
| 幂等 / 去重 | "要确保只处理一次" | **幂等键 + 唯一约束**，不是锁 |
| 昂贵计算去重 | "防止并发重建" | **single-flight（单飞请求合并）**，不是锁 |
| 跨进程排他写 | "要锁住资源" | DB 行锁 / advisory lock / 乐观并发，不是分布式锁 |

**计数基底：锁 / 信号量 / 计数器 / 限流是同根原语。** 它们不是并列的东西，而是一个**计数基底**按用途分化——做**许可控制**（互斥锁 N=1、信号量 N、限流按时间窗）必须**准确**，计错一个就可能把两台机器同时放行；做**状态统计**再按需求分**精确**（成交额要账平）与**近似**（UV/去重本就无需精确）。限流虽属许可控制家族、本可精确（精确令牌桶），但因**有损可容忍** + 网关低延迟而**选择**端侧本地近似——同一家族，确定性成本值不值完全不同。

这条底座直接推论：**连"计数"都常无需精确，何况"互斥"？** 真正需要精确互斥的场景，在排除了这个家族里的近似成员后更加稀少——这才引向下面的替代方案。

**真正的分布式互斥极其稀少**。一旦排除上述常用误解，剩余的需要"多个进程对分布式资源做互斥写且无自然幂等键"的窄口，才能和"分布式锁"相关——而即便这里，乐观并发（version token）也常常能绕开。

## 2. 文件锁的分界：系统层合理，平台层反智

「分布式锁是反模式」这个判断的反面：**什么时候"锁"才是合理的？** 分界点在**媒介**与**部署形态**。

### 系统层：基于文件系统的协调是合理的

在**系统层**交互（如包管理器 `package-lock.json`、PID 文件、`/var/run` 锁文件）里，基于文件系统的协调是**合理**的——因为文件系统本身是**跨进程、跨重启存在的持久共享媒介**，系统级协调依托它很自然。

> 注：真正的 `flock`/`fcntl` advisory lock 内核绑定在进程/打开的 fd 上，进程退出即释放；这里指靠**文件存在性**表达状态、且能重启后继续依托同一媒介的机制。

### 编程平台层（单副本）：进程内 mutex 足够，文件锁是反智

到了**编程平台层**的**单副本 + 平台兜底重启**场景（如 K8s 单个 Pod 由编排层拉起），应用自己**无需处理宕机**——进程内 mutex 即足够，再用文件锁/进程锁式思维，就是**反智**的。这类低门槛协调习惯在 PHP 生态（共享托管处境的代表，多进程共享块文件系统、无常驻协调服务）最显眼。

### 真分布式多副本：需要共识/lease，不是锁

**真分布式多副本**是另一回事——多个活跃副本共享资源的互斥写仍须**跨节点**保证，只是正确方法是**共识/lease**，不是 Redis 锁。见 §7。

## 3. 反模式案例：审核锁定的误用

**常见反模式**：清单审核时锁死条目，禁止新增。

```text
❌ 审核时 lock(list) → 审核期间不许 add
   - 不必要地阻塞业务（新增被卡住）
   - 把"审核"这个只读/快照操作，做成了全局排它
```

**正确：水位线模式（watermark）**。审核只看当前快照，审核期间的新增按审核时间点过滤，继续纳入后续批次：

```text
✅ 审核水位线 = T0
   - 审核 T0 之前的条目
   - T0 之后新增的 → 按 进入时间 > T0 筛选，进入下一轮审核
   - 无锁：新增永不阻塞
```

在 Flux 这类实时架构里，甚至可以在审核视图内直接新增，但这种方式会干扰当前审核焦点——所以水位线仍是更优解。**但无论哪种，直接锁死都不是最优策略。**

## 4. 分布式事务更需要无锁

分布式事务有多种协议（两阶段提交、Saga、TCC、本地消息表/事务性 Outbox、事件源），它们处理的是**一致性**，不是互斥。这些模式本身的焦点是协调与补偿，**用不到分布式锁**。

把分布式锁塞进分布式事务，是用"阻塞"去掩盖"协调缺失"——方向就错了。

## 5. 单机已是如此，分布式是放大器

即便在单机/单进程编程里，**大多数场景也应该避免锁**（优先无锁数据结构、乐观并发、消息传递）。分布式锁把这种低效**放大了几个数量级**：

- 单机锁：纳秒级阻塞，本地无网络。
- 分布式锁：一次获取含网络 RTT + 共识往返，微秒~毫秒级，还得处理时钟同步、GC 停顿、网络分区、锁过期。

**引入的问题远多于解决的问题。**

## 6. Redis/Redlock 在此的定位

Redis 常被辩护为"适合分布式锁"。这条辩护在两层崩塌：

1. **锁的正解本来该是共识/lease**（etcd、Consul、Fjall+Openraft），天然强一致。
2. Redlock 连这个"次优解"都不合格——不满足严格互斥的正确性保证。详见 [Redis 批判 §6.1](redis-critique.md)。

## 7. 真正需要"共识"而非"锁"

当确实需要"多个进程对分布式资源的强一致互斥写"，正确工具是**共识层提供的 lease / 领导权**，不是分布式锁。共识保证一致性，lease 提供所有权，二者结合天然处理节点故障与信任转移——这正是「共识与协调层级」的职责，见 [Kv 存储引擎](kv-storage-engine.md)。

## 交叉引用

- [Redis 批判 §6.1 分布式锁 / Redlock 论战](redis-critique.md)
- [共识协议](consensus-protocol.md)
- [Kv 存储引擎 — 共识与协调层级](kv-storage-engine.md)