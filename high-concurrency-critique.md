# 「高并发」批判

## 核心论点

「高并发」不是一个工程概念，而是简中互联网特定历史阶段的话语产物。它把一组本应拆开的具体工程问题（吞吐、在线连接数、请求速率、任务并发度）封装成一个模糊的「能力名词」，其流行标示的不是技术能力，而是「缺什么就喊什么」的话语结构。

## 词汇解剖：英文里没有对应词

英文技术语境只有指向具体问题的词：throughput（吞吐）、concurrency（并发度）、request rate（请求速率）、C10K（万级连接问题）、horizontal scaling（水平扩展）。每个词量纲明确、可度量。

「高并发」没有量纲。它是能力声明，不是度量——说「我们系统支持高并发」无法证伪，说「我们系统承载 5 万 QPS / 20 万长连接」才能讨论。一个不可证伪的词能成为行业标准话语，靠的不是技术精确性，而是传播结构。

## 流行的三个来源

- **流量爆炸时代的技术图腾**：2010 年代初，12306、双 11 秒杀、春节红包是中国互联网独有的极端瞬时流量场景。第一代大厂技术布道者（淘宝、京东的架构分享）反复以「高并发」为叙事核心，它成为那个时代的技术图腾。同期英文世界的对应叙事是云原生与 scale-out，词汇完全不同。
- **面试文化的自我繁殖**：简中技术面试把「高并发」设为标准考察项，迫使每个候选人背诵 Redis、消息队列、分库分表的「高并发三件套」。面试需要标准答案，标准答案需要标准词汇——词汇靠面试循环自我繁殖，与真实工程场景逐渐脱钩。
- **fpm 模型的防御性焦虑**：PHP 的 per-request 重置模型在这套话语下天然有原罪。Swoole 的整个营销叙事就是「让 PHP 也能高并发」——这个词在 PHP 语境里恰恰是对 fpm 模型的防御性反应。稀缺性创造话语。

## 为什么其他语言用户不谈

- Go 用户谈 goroutine cheap、built-in concurrency——语言特性层，不是需要征服的性能山头。
- Rust 用户谈 throughput、zero-cost abstraction。
- Java 用户谈 virtual threads 降低 thread-per-request 成本。
- 对这些生态，「能并发」是出厂默认，没有可「谈」的稀缺性。只有当某物稀缺且难以获得时，它才值得被反复谈论——「高并发」的话语热度本身就是稀缺性的证据。

## 应试工程的孪生体

「高并发」话语与「应试工程」（见 tech-philosophy.md）同构：

- 制造考卷：把瞬时峰值流量定义为技术优劣的考场（秒杀系统）。
- 钻营考纲：面试三件套成为标准解法，不管业务是否需要。
- 结果：为不存在的峰值流量预付架构复杂度——绝大多数系统终其一生达不到需要消息队列削峰的请求量。

精确的工程表述应该是逐项拆开的：目标吞吐多少 QPS、长连接多少万、单请求 p99 延迟多少 ms、可接受的横向扩展成本——每个都是可度量、可证伪、可验证的工程量。当讨论落到这些量纲上，「高并发」这个词就该退场了。

## PHP 案例本位：话语与技术事实的分离

PHP 是唯一一个「每请求重置」至今仍是绝对主流形态的语言生态：其他语言的同步 pre-fork 模型（Python WSGI、Ruby sync worker）早已是被淘汰的遗留形态，而 fpm 至今占据 PHP 部署的绝对主流，Workerman/Swoole 在 PHP 内部是极小众。这使 PHP 社区成为「高并发」话语最活跃的群体——正因为他们的默认形态在这套话语下最不占优。

技术事实是：低并发是 PHP 的默认部署形态，不是语言属性上限。同一个 PHP，换到 Workerman（gateway/worker 常驻进程模型）上，单机数万连接。把部署形态的默认值当成语言能力的天花板，正是「高并发」话语制造的认知混淆。

## 结语

「高并发」一词的价值在于它标记了使用者的语境：面试体系内、简中技术圈、以峰值流量为荣的叙事传统下。跳出这个语境，工程问题从来都是具体的——把模糊的能力名词还原为可度量的工程量，是走出应试话语的第一步。

---

## 交叉引用

- tech-philosophy.md — 「应试工程」总纲，本文档的「高并发」话语是其词汇层表现
- redis-critique.md — Redis 作为「高并发三件套」首件的批判
