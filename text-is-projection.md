# 文本是一维投影：信息的结构、图谱化与思维导图

## 核心论点

**信息有结构，图谱是以关系为组织方式的结构。** 文本则是高维结构在一维载体上的投影——文本化的过程**消除了结构**，把结构转换为线性文本，维度压缩到一维。这就是为什么**表格的表现力比段落强**：它是二维的，比一维多保留了一维。

由此，"文本是否足够表达结构化信息"不是偏好问题，而是**物理限制**。文本作为投影，有系统性缺陷——这些缺陷不是文风问题，无法靠更好的写作消除。

## 文本的两个系统性根源缺陷

讨论"为什么要用图谱/表格替代纯文本"时，先分清两个**独立、不可互相推导**的失败模式。判定依据：它们不可互相推导。

### 根源一：间接性 —— 过程的税

**结构不被文本载体持有。** 一个知识点有三个注意事项，这个"三个"的结构**不在文本里**，要靠读者的智力资源（在思维中）提取维护。于是一切对该结构的操作都必须经由智力转译：

- **消耗性维持**：结构靠心智持续提取维护，随时可能遗忘/失真，成果不沉淀。
- **间接操作**：修改（增/删/改）要先在思维中调整结构，再转写成线性文本。
- **一致性差**：还要考虑上下文一致——实体更名要改所有引用，而文本搜索并不总能解决（语义等价但措辞不同的引用搜不到）。

这些是"间接性"在不同操作（维持、修改、计算、校对）上的实例——不是并列的缺陷，而是同一根源的各面。计算、维持、一致性问题统被一个词罩住：**结构不随文本在场，一切操作经智力资源转译，每次重付。**

**对偶**：图谱中结构由节点和边直接承载，操作直接落在结构上，无智力转译、无每次重付。

### 根源二：投影的不可逆性 —— 存量的债

**投影一旦发生就不可逆** "高维→一维"这个投影不可逆。它混着两种独立的损失：

| 子性质 | 含义 | 本质 |
|:--|:--|:--|
| **维度损失** | 高维→一维，被压掉的维度不在文本中，任何智力资源都还原不出 | **几何性的信息丢失**（三维压成一维的必然） |
| **导向性** | 投影角度是人为选择的，选择本身携带立场 | **投影者引入的自由度**（同一段事实，先说因还是先说果） |

**关键辨析**：这两个不是一回事。"维度损失"严格指几何性的压扁——同一维度也可以选角度（先说因 vs 先说果），那是**导向性**，不是维度问题。导向性是投影者人为选择的自由度，**不完全受几何必然约束**。**这个辨析精确化了"导向性"，让"维度损失"保持几何上的严格含义。**

- **投影损失**：维度被压掉的部分不在文本中——拍照只能看到压扁的一个面，缺很多信息。
- **视角可操纵**：更重要的是，**这个角度可以人为选择——表达即权力**。

**对偶**：图谱无预设拓扑，**所有角度同时存档**——模型的每个面都存在，不需要选角度，也不需要读者在脑中旋转它。

## 完整对偶：图谱为何好

图谱同时消除两个根源：

- **节点和边让结构在场** → 消解间接性（操作直接落于结构，无智力转译）。
- **无预设的拓扑让所有角度同时存档** → 消解投影不可逆（无维度损失、无路径选择）。

**"照片 vs 模型"比喻精确对位**：文本是照片——压扁了（维度损失）+ 拍摄者选了角度（导向性）；图谱是模型——每个面都真实在场，无需选角度，无需读者脑内旋转。

## 中间态：思维导图

思维导图（整理思路）流行的原因正在于此：

- **非消耗性维持**：结构由图的形状直接承载，不必靠心智维护。
- **可直接操作**：增删节点即改结构，无需转写线性文本。
- **不用考虑上下文**：视觉结构即上下文，无需文本式"引用一致性"。

**限制与定位**：思维导图只能表达**树形结构**——一个节点一个父节点，无法表达网状关系（多父、环、跨子树引用）。它的信息复杂度介于文本与表格之间：**比一维多，但不到 1.5 维**——树有分支，路径数高于线性（1 维）的文本，但又是层次而非二维的 grid。而**图谱（图）本身没有维度**——它是关系拓扑，节点间由边连接，不构成几何坐标空间，硬把"维"套在图上是范畴错误。树是文本（1 维）与表格（2 维）之间的中间态，图谱则不属于维度谱系、另起关系组织的框架。

## wiki：更高层次的投影

wiki 是在**更高层次**上的结构化——它不追求单文档内的图谱化，而是通过**跨文档链接、超图结构**在"文档集合"这一层组织结构。它把"结构化"的粒度从段落/知识点提高到文档/主题，是文本之上的第二层结构组织。

## 交叉引用

- **[graph-memory.md](graph-memory.md)**：本篇讲"文本投影的缺陷"，graph-memory 讲"用图谱做记忆以消除这些缺陷"——互为表里。
- **[property-graph-queries.md](property-graph-queries.md)**：图谱作为载体的存储与查询实现。
- **[tag-forest.md](tag-forest.md)**：树形组织范式（思维导图一档的通用化）。