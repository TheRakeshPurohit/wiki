# 文档格式对比：Markdown 与下一代纯文本标记语言

> 一份 AI 生成对比文被批判后的整合。原稿把 Neorg / Org-mode / Typst / AsciiDoc 拉平成"与 Markdown 竞争的四路豪强"，但它们的文档**用途根本不同**。本文按文档用途分轴组织，而非沿用原稿的一维竞争矩阵。

纯文本标记语言分散在几条用途不同的轴线上，各轴含一个 Markdown 参照和若干替代。按用途分轴能避免把不同目的的工具强行并列：

- **笔记轴**：Markdown / Neorg (Norg) / Org-mode —— 用于速记、个人知识库、大纲、任务。
- **排版轴**：Typst —— 用于学术论文、简历、精美排版（LaTeX 的替代，非笔记格式）。
- **出版轴**：Markdown / AsciiDoc —— 用于出书、超长技术文档、API 官方文档。

---

## 一、贯穿各轴的共同批判点：Markdown 的视觉缩进

Markdown 最实质的设计缺陷是**用视觉缩进表达嵌套层级**。二级无序列表靠行首多加空格，渲染器据此推断层级。问题在于：

- 层级信息由"数了几个空格"承载，肉眼和机器都容易产生二义性。
- 编辑器换行、粘贴、缩进异常时，嵌套结构悄悄错乱，且不易察觉。
- 不同方言（CommonMark / GFM / 各后端）对缩进规则的处理不完全一致，方言乱飞。

替代格式的共性改进方向是**符号驱动 + 显式闭合**：层级深度由符号数量表达（Neorg），或由显式闭包界定（Typst 代码块 `#`、AsciiDoc `====`）。排版和渲染交给工具，不依赖用户手工对齐。

> 注意：这是对 Markdown 的真实翻盘点，但报告里有一处稻草人——"有序列表需手动算数字、Neorg `~` 自动递增是优势"。不成立：Markdown 写 `1.` 即按序自动渲染，无需手工编号。这个优点不应计入。

---

## 二、笔记轴

### 2.1 Neorg (Norg) —— Neovim 内的符号驱动笔记

**定位**：Neovim 专属的标格式，被设计为现代 Markdown 替代，推行"无空格缩进、符号驱动、显式闭合"。

- **无序列表** `-- 列表项`（符号数量表深度，不用前导空格）
- **标题层级** `*` / `**`（星号数量）
- **显式闭合** `_` 降低一级、`@end` 结束区块，明确告知解析器层级边界
- **行内粗体** `*文本*`、**行内代码** `~代码~`
- **超链接** `{URL}[文字]`（链接在前、描述在后，与 Markdown 相反）
- **元数据** 原生对象标签 `@document.meta`，属性与内容在语法层隔离

**强约束**：基于 Tree-sitter，语法确定性高——层级完全由符号数量决定，渲染器代劳，不用对齐空格。

**缩进黄金法则**（Neorg 下处理嵌套的实操）：

1. 不用空格推列表符号；写二级列表直接在行首打 `--`。
2. 多层嵌套结束时，在新行敲 `_` 回车，显式"降低一级"，不污染后续正文。
3. 插入模式用 `Alt + Right` 升级列表（`-` → `--`）、`Alt + Left` 降级。

**生态短板**：深度绑定 Neovim，跨平台可用性差；树语法对前端渲染要求高（需 vite-plugin-norg 或把 tree-sitter-norg 编译为 WASM）。

### 2.2 Org-mode —— Emacs 体系的笔记操作系统

**定位**：随 Emacs 诞生二十余年，Neorg (Norg) 的精神教父，是极客纯文本知识管理的图腾。

- **不单是标记语言**：是包含 Agenda（日程表）、时间追踪、文学编程（Babel，在文档内运行各语言代码并返回结果）的一体化系统。
- **多维连接**：双链跳转、大纲视图折叠、内建表格计算（伪 spreadsheet），纯文本格式中能力最全。
- **生态绑定**：与 Emacs 深度耦合；虽然 2025 年周边有 deminimized 的浏览器端 org.js，核心优势仍锁定在 Emacs 生态。

**取舍**：能力上限最高，但历史包袱重、语法繁杂；非 Emacs 用户享受不到其生态红利。

### 2.3 笔记轴小结

Norg 与 Org-mode 是**同一用途的两种实现**，不是两套独立世界观——Org-mode 是纲领，Norg 是 Neovim 用户对同一理念的现代重写。新用户权衡点是**编辑器绑定**（Neovim 用 Norg，Emacs 用 Org-mode），而非语法本身。

---

## 三、排版轴：Typst

**定位**：旨在取代 LaTeX 与 "Markdown + Pandoc" 组合的现代排版系统——注意这是**排版**，不是笔记格式，不与其他三者在同一赛道。

- **渲染速度**：Rust 编写、增量编译，毫秒级实时生成 PDF 预览。
- **现代编程能力**：内置强类型脚本语言，支持函数、变量、条件判断；写模板和复杂排版比任何标记语言灵活。
- **原生公式**：`$sum_(i=1)^n$` 直接代码化数学符号，无需加载 MathJax。前端集成潜力大——编译器能编译为 WASM 在网页端毫秒级渲染。

**定位误区澄清**：Typst 不能直接替代 Markdown 的速记/笔记角色——它是"产出成品文档"（论文、简历、报告）的工具，输入成本远高于速记。

---

## 四、出版轴：AsciiDoc

**定位**：专为出书、大型技术白皮书、API 官方文档设计。

- **托管结构**：原生支持 include（包含文件）、多层级交叉引用、多重脚注、自动生成高度定制的目录——Markdown 这些要依赖插件魔改。
- **严密表格与提示框**：跨行跨列、单元格内嵌套列表/图片，原生 `WARNING` / `NOTE` 等语义化排版元素。
- **生态**：Antora / Ruby 体系，JS 编译器 Asciidoctor.js 成熟，前端 SPA 解箱即用。

**定位误区澄清**：AsciiDoc 在"出版文档"线与 Markdown 同族（都是面向成稿的标记），但电流质量、工具链条完整度远高于 Markdown 的魔改插件组合——它是给"要写书"的人用的，不是给随手速记的人用的。

---

## 五、AI 友好度：作为待验证论点，而非结论

原稿把"AI 友好度"做成对比表的一列（Markdown 🟡 / Neorg 🟢 / Typst 🟢…），这是**对称填充的主观格，无实测依据**。其中确实有一个有讨论价值的论点，值得单独拎出、标注不确定：

**论点**：AI 的 Token 预测"害怕"模糊逻辑（Markdown 英文混排下划线失效）与空间定位（数空格对齐嵌套）。因此符号驱动的格式（Norg 的符号计数 + 显式闭合、Typst 的代码式组织）比 Markdown 更利于 AI 稳定生成成对的 Token，不需要数物理空格。

**反证与不确定性**：

1. **语料垄断的现实**：LLM 训练语料以 Markdown 占绝对主导，现阶段给 AI 喂 Norg / Typst 源码会混入 Markdown 语法产生幻觉——原稿自己承认这点，却又话锋一转断言"未来真理是 Norg 更友好"，这是先自证再强行翻盘，逻辑不成立。
2. **"符号驱动更利于 Token 预测"未经实测**，属推测而非结论。LLM 处理自然语言/代码的表象差异（"处理代码逻辑性更强"）本身也是观察，不是通用规律。

**结论**：现阶段 AI 生成内容的最稳妥选择仍是 Markdown（语料优势压倒语法优势）；Norg / Typst 的"AI 更友好"是**待语料充分后的假设**，不构成当下迁移的理由。

---

## 六、选型导引

| 需求 | 推荐 | 理由 |
|:--|:--|:--|
| 随手速记、网络发帖、通用文档 | **Markdown** | 生态统治级、处处可用；AI 语料优势，现阶段最稳妥 |
| Neovim 内个人知识库、追求语法确定性 | **Neorg** | 符号驱动解决缩进二义性；但锁定 Neovim 生态 |
| Emacs 一族、重度任务管理 + 代码混编 | **Org-mode** | 能力上限最高，Agenda/Babel/表格内建 |
| 论文、简历、精美成稿排版 | **Typst** | 现代排版系统，干"产出成品文档"的活 |
| 出书、超长技术文档、API 文档 | **AsciiDoc** | include/交叉引用/语义框完整，专给成稿文档 |