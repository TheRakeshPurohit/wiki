# 自动机、状态机与 reducer：术语的两种语域

## 一句话

**自动机与"状态机"在形式上是同一个对象；但在工程的"状态机模式 / 类库名"语境里，"状态机"有更窄的意思，只指"命名状态 + 按状态限制操作"的那一类。** 复制状态机（RSM）的 reducer 属于前者，不属于后者。

## 1. 形式语域：自动机 = 状态机

同一个数学对象——确定性自动机 `M = (Q, Σ, δ, q0, F)`：

| 符号 | 含义 |
|:---|:---|
| Q | 状态集（可无穷，见下推自动机、图灵机） |
| Σ | 输入字母表 |
| δ | 转移函数 `δ : Q × Σ → Q` |
| q0 | 初始状态 |
| F | 接受状态集 |

"自动机"是**可计算视角**（识别什么语言、算什么）；"状态机"是**业务视角**（建模领域的状态转移）。两者是同一对象的两个称呼，`δ : Q × Σ → Q` 就是编程里的 **reducer**。

`Q` 可以是无限集合（下推自动机的栈、图灵机的构型都是无限态），因此"状态没有清晰边界、操作没有限制"并不 disqualify 它是自动机。

## 2. 工程语域：「状态机」只指受限操作那类

在"状态机模式"（State pattern）和状态机类库（xstate、spring-statemachine 等）里，"状态机"一词已结晶成一个更窄、更实用的意思：

- 一组**命名状态**；
- 每个状态定义**哪些操作合法**（转移守卫）；
- 触发操作后跳转到另一个命名状态——**状态转换 + 限制操作**。

这是自动机的**受限子类**（工作流式 FSM / 带标签的转移系统），也是工程上"状态机"这个词真正承载信息的地方。

## 3. 空泛性测试：为什么不能什么都是「状态机」

若把"状态机"用在一般意义上——web 应用也叫状态机（接口=操作、数据库=状态）——那几乎一切皆是，术语随之失去区分度。这是决定性的空泛性测试：**要把"状态机"保留给"限制操作的那类"**，一般性的 `fold 命令到大数据状态` 另用一个词（见下）。

## 4. RSM（复制状态机）的定位

复制状态机的 `apply`（reducer）是（大状态空间、可无限）确定性自动机的转移函数 δ——**形式上是自动机**。但它**没有**工程语域"状态机"的特征：无命名状态、无"此状态只能做哪些操作"的转移矩阵、无离散状态的跳转。因此在工程语域里，描述它的准确词是 **reducer**。

| | 自动机（形式） | "状态机"（工程） | reducer / RSM |
|:---|:---|:---|:---|
| 状态 | 任意集合，可无限 | 离散命名状态（有限） | 大数据状态 |
| 转移 | `δ : Q×Σ→Q`，定义在整个字母表 | 按状态限制合法操作 + 跳转 | 任意命令折叠 |
| 典型 | web 应用、raft、一切 | 工作流、协议、UI | 复制状态机 `apply` |

## 5. 结论

- **形式语域**：自动机 = 状态机，RSM 是自动机，`apply` 是它的转移函数。
- **工程语域**："状态机"指受限操作的工作流 FSM，RSM 不算，故用 **reducer** 更贴切。
- 两语域分别真——不要用形式语域去否认工程语域的窄义，也不要反过来。

## 交叉引用

- [共识协议](consensus-protocol.md)：复制状态机实现视角（reducer、日志仓库、快照压缩）§7
- [KV 存储引擎](kv-storage-engine.md)：Raft 原理与边界