# 自动机、状态机与 reducer：术语的两种语域

## 一句话

**自动机与"状态机"在形式上是同一个对象；但在工程的"状态机模式 / 类库名"语境里，"状态机"有更窄的意思，指"一个实体自身的命名状态 + 按状态限制操作"的那一类。** 复制状态机（RSM）的 reducer 属于前者，不属于后者；agent 编排这类"过程/多实体"用语是工作流图，不是状态机。

## 形式语域：自动机 = 状态机

同一个数学对象——确定性自动机 `M = (Q, Σ, δ, q0, F)`：

- `Q` — 状态集（可无穷，见下推自动机、图灵机）。
- `Σ` — 输入字母表。
- `δ` — 转移函数 `δ : Q × Σ → Q`。
- `q0` — 初始状态。
- `F` — 接受状态集。

"自动机"是**可计算视角**（识别什么语言、算什么）；"状态机"是**业务视角**（建模领域的状态转移）。两者是同一对象的两个称呼，`δ : Q × Σ → Q` 就是编程里的 **reducer**。

`Q` 可以是无限集合（下推自动机的栈、图灵机的构型都是无限态），因此"状态没有清晰边界、操作没有限制"并不 disqualify 它是自动机。

## 工程语域：「状态机」只指（单实体的）受限操作那类

在"状态机模式"（State pattern）和状态机类库（xstate、spring-statemachine 等）里，"状态机"一词已结晶成一个更窄、更实用的意思：

- **一个主体（实体）**的多种驻留/状态；
- 每个状态定义**哪些操作合法**（转移守卫）；
- 触发操作后跳转到另一个命名状态——**状态转换 + 限制操作**。

这是自动机的**受限子类**（工作流式 FSM / 带标签的转移系统），也是工程上"状态机"这个词真正承载信息的地方。它必须**单实体**——跨实体的"谁在干什么"不是状态机，见下节「状态机 vs 工作流图」。

## 状态机 vs 工作流图：单实体与过程

「限制操作」还不是完整判据——更深的轴是**单实体 vs 过程**：

- **中心**
  - 状态机：一个实体的多种驻留/状态。
  - 工作流图：过程的步骤顺序 / 流程 / 路由。
- **节点语义**
  - 状态机：同一对象的模式（订单：待付 / 已付 / 已取消）。
  - 工作流图：步骤 / 角色 / 组件（谁在做什么）。
- **转换**
  - 状态机：实体状态转移，保留该实体身份。
  - 工作流图：步骤流转，可跨多个实体 / 参与者。
- **典型**
  - 状态机：协议、订单、Actor 生命周期。
  - 工作流图：BPMN / petri-net / 编排图。

状态机的状态是**同一个东西**的驻留模式；工作流图的节点是**步骤 / 工人**，不是同一个东西的"状态"——所以工作流图天然不是状态机，这个区分与"限制操作"无关（形式化里对应 FSM vs workflow-net / petri-net）。

**AI Agent 编排**（LangGraph `StateGraph`、OpenAI/Anthropic 工作流）是典型**工作流图**——节点 = LLM 调用 / 工具 / 子 agent，边 = 路由，跨步骤甚至多实体，不是"单实体的状态机"。真正能叫"状态机"的是 ReAct 循环、工具调用内部那种**单一 agent 自身的生命周期**（think → act → observe）。

## 空泛性测试：为什么不能什么都是「状态机」

若把"状态机"用在一般意义上——web 应用也叫状态机（接口=操作、数据库=状态）、编排图也叫状态机——那几乎一切皆是，术语随之失去区分度。这是决定性的空泛性测试：**要把"状态机"保留给"单实体的、限制操作的"那类**；过程/多实体用 **工作流图**，一般性的 `fold 命令到大数据状态` 用 **reducer**。

## RSM（复制状态机）的定位

复制状态机的 `apply`（reducer）是（大状态空间、可无限）确定性自动机的转移函数 δ——**形式上是自动机**。但它**没有**工程语域"状态机"的特征：无命名状态、无"此状态只能做哪些操作"的转移矩阵、无离散状态的跳转。因此在工程语域里，描述它的准确词是 **reducer**。

按维度看四个概念（自动机 / 工程"状态机" / 工作流图 / reducer）：

- **状态**
  - 自动机：任意集合，可无限。
  - 工程"状态机"：离散命名状态（有限）。
  - 工作流图：步骤（可跨实体）。
  - reducer：大数据状态。
- **转移**
  - 自动机：`δ : Q×Σ→Q`，定义在整个字母表。
  - 工程"状态机"：按状态限制合法操作 + 跳转。
  - 工作流图：边 + 条件路由。
  - reducer：任意命令折叠。
- **典型**
  - 自动机：web 应用、raft、一切。
  - 工程"状态机"：协议、UI、订单。
  - 工作流图：agent 编排、BPMN。
  - reducer：复制状态机 `apply`。

## 结论

- **形式语域**：自动机 = 状态机，RSM 是自动机，`apply` 是它的转移函数。
- **工程语域**："状态机"指**单实体**的、受限操作的工作流 FSM；RSM 不算（用 **reducer**），跨实体/过程也不算（用 **工作流图**）。
- 三词归位：**状态机**=单实体自状态；**工作流图**=过程/多实体；**reducer**=大数据 fold。
- 两语域分别真——不要用形式语域去否认工程语域的窄义，也不要反过来。

## 交叉引用

- [共识协议](consensus-protocol.md)：复制状态机实现视角（reducer、日志仓库、快照压缩）
- [KV 存储引擎](kv-storage-engine.md)：Raft 原理与边界