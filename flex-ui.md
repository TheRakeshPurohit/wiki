# Flex UI：传输 UI 的事件驱动声明式框架

## 定位

Flex UI 是一套**前端/UI 框架设计原则**，与一般性的数据传输架构互补：

- **[flux-architecture.md](flux-architecture.md)** 描述**一般性原则**——存量数据如何流动、服务端如何推送、客户端如何按 id 增量消费。
- **Flex UI 把同一套原则用于"传输 UI 本身"**：UI 不是渲染脚本/请求-响应的产物，而是**作为数据结构在 WS 上以增量指令流传输**。它是框架性的东西——描述"UI 由数据驱动"的整套形态，而非某个具体引擎的实现。

Fluxora 是这套框架的一个具体实现（事件驱动的 UI 总线，Rust/Dioxus，见 [projects/fluxora-architecture.md](projects/fluxora-architecture.md)）。Flex UI 是它抽象出的原则层；实现细节（编解码器、Kafka、具体 crate）留在项目文档，不在此重复。

## 核心概念：UI 作为数据结构

UI 不是"页面 + 脚本"，而是**一棵有类型的组件树（Brick），以 JSON（或更紧凑的二进制）表示，通过事件流传输**。关键推论：

- UI 是**可序列化的数据**，不是不可传输的程序。
- 组件**绑定到事件名称**，而非 API URL、无 HTTP 动词。Input = 发出事件（写），Display = 监听事件（读）。网关自动把事件名映射到传输主题。
- 传输的**是增量指令，不是完整快照**（与 flux-architecture 的消费侧原则一致）。

## 为什么 UI 由数据描述，而非代码/HTML

| 维度 | AI/业务生成 HTML | AI/业务生成 JSON（Flex UI） |
|------|------------------|----------------------------|
| Token 消耗 | 高（大量标记样板） | 低（JSON 比 HTML 小一个数量级） |
| 输出稳定性 | 每次不同（样式漂移） | 确定性（样式由框架锁死） |
| 数据验证 | 无 schema，靠视觉检查 | JsonSchema 编译期验证 |
| 生产者职责 | 内容 + 结构 + 样式（全包） | 只管内容和结构（样式交给框架） |

**本质**：把"创意决策"（样式）从生产者（AI/业务）手里拿走，只让它做擅长的事（内容和结构）；渲染交给确定性代码。面向消费 Web 的 HTML 由浏览器即时渲染，无 schema、每渲染一次样式可能漂移；而 Flex UI 用可验证、可缓存的 JSON 数据流，把"交付 UI"变成"交付结构化数据"。

## 声明式 DSL：Brick

- **类型化枚举**，零歧义序列化（tagged），生产者输出 `{"type":"button",...}` → 自动反序列化为对应组件类型。是所有组件类型的单一封闭词表（见「组件系统：封闭集」）。
- **静态子树**：`sub: Option<Vec<Brick>>` 声明式嵌套的**固定子节点树**，`Frame` 直接递归展开渲染。
- **列表项模板**：`item: Option<Vec<Brick>>` 是列表型组件（Fold/Rack）的**候选模板集合**——配合 `selector` 对列表数据逐项选模板渲染；它**不是**列表数据本身（数据来自 `list` 注册表经 bind source），而是"给列表项挑选用哪个模板"。注意不要把它误当成"列表项"。

### 递归节点树：渲染与实例化

**递归渲染（`Frame`）**：一棵 Brick 树的渲染就是深度优先展开——组件 `Frame` 检查当前 Brick 的 `sub`（子节点列表）：有则把每个子节点递归包成子 `Frame`，连同自身一起交给类型分发；没有则直接交给类型分发。于是整棵 JSON 树被逐层摊平成实际的 Dioxus 组件树，结构由数据（`sub` 递归）而非代码决定。

**类型分发（`Dynamic` + `gen_dispatch!`）**：`Frame` 摊平后，`Dynamic` 负责按照 Brick 的类型枚举把当前节点**派发到对应的具体组件**（case/rack/button/input…）。分发由过程宏 `gen_dispatch!` 从 `Brick` 枚举 AST 自动生成 match（而非手写）——它面朝的是**封闭枚举**，所以能编译期穷举全部组件，每类硬派到自己的渲染函数，无运行时注册、无 trait object 开销（详见「组件系统设计权衡」）。

**模板实例化（`Render {name, data}`）在入注册表前完成**：模板机制把数据填进已注册模板,产出**新的 Brick**。`Brick::render(env)` 在 **dispatch 阶段、写入注册表之前**被调用(见 `store.rs`:每种内容动作都是 `render(&env)` **先**于 `layout.set`/`data.insert`/`list` 合并)——遇 `Brick::render(r)` 变体时用 `r.name` 取模板、以 `r.data` 渲染成文本、**反序列化回 Brick 替换自身**,并**在数据落库前随整个子树递归展开完**,使 incoming 树中不再残留 `Render` 变体。故渲染阶段(`Frame`/`Dynamic`)只见到已展开的 Brick,**模板实例化是"收到数据、进注册表之前的预处理",不是渲染时的运行时展开**。模板可嵌套——一个模板渲染出的 Brick 若仍含 `Render`,在同一趟展开中一并展开。

三者关系与时机关联：**`Frame` 负责树的形状（渲染期递归摊平），`Dynamic` 负责节点形态（渲染期按类型分发），`Render` 在数据到达即预处理展开（入注册表前）**——形状与形态在渲染期决定，参数（模板展开）在数据入口就绪。

### Bind 系统：组件 ↔ 事件流

```json
{
  "bind": {
    "input":  { "event": "chat.send", "type": "text" },
    "output": { "source": "chat.msg" }
  }
}
```

- **source**：监听事件流（读）。
- **event**：发出事件（写）。
- **target / field / submit**：高级绑定模式。

无 API URL。网关把事件名映射到传输 topic，组件与后端服务通过事件解耦，彼此透明。

### 绑定数据源机制（消费侧实现）

组件自身的 `bind` 定义如何拿到数据，取决于它是**单项组件还是列表组件**——前端在内部解析 schema 中的 `bind`，到**全局注册的数据视图**查询：

- **单项组件**（`container` 等）：解析 `bind` 的 `Source{variant}` 得到 `source`（事件名），到全局 `data` 注册表（`data: HashMap<String, Brick>`，值是**单个 Brick**）中 `data.get(source)` 取出，作为组件内容渲染。当前 UI 树本身是独立的 `layout` 字段（`layout: Signal<Brick>`），与 `data`/`list` 三个并列——顶层树在 `layout`，按 source 动态取的数据在 `data`/`list`。
- **列表组件**（`rack` 等）：`source` 到独立的 `list` 注册表（`list: HashMap<String, Vec<Brick>>`）查列表，逐项渲染。

**数据注册表与列表注册表必须分开**——可以统一，最简单的办法：**集合优先**——全部按列表处理，不需要列表的组件只放一个即可。但**列表组件的列表语义和单项组件不同**：到 `data` 里拿列表、或到 `list` 里拿单项，语义上都是错的。二者分表是刻意为之。且许多组件确实不是列表语义。故维持 `data`/`list` 分表。

**发送机制**：组件的写操作就是取 `bind` 定义里的 `event`，连同**当前数据**（组件本地的值）`send` 出去（见 store 的 `send(event, id, content)`）。`input` 按下 Enter 即 `send(event, None, val)` 发事件。

**组件联动（组件间直接同步）**：不经过后端，通过 `bind` 数据里的 **signal**（Dioxus 信号）同步。`form` 遍历子树时，把每个 `Field{field, payload}` 的 `bind` 改写成携带**局部 signal**（存该字段当前值）；提交时把 scope 内所有 `(field: (signal, payload))` 收集成 `content`，一次性 `send(event, ...)` 出去。同类机制可用于任意组件对——共用 signal 即建立同一数据源的响应式联动。

#### `selector`：列表项渲染的分派键

`selector` 是每个组件 `attrs` 的可选字段（`get_selector()` 暴露），本身不渲染，只在列表项模板选择时被消费——作用是**让同一个列表能按项渲染异构组件形态**。机制分两段：

1. **构建模板索引**（`ItemContainer::from`，消费 rack 的 `item` 候选模板集合）：凡带 `selector` 的模板放进 `index: {selector → Brick}`；不带 `selector` 的作为 `default`。
2. **逐项选择**（`select(child)`，渲染每个实际数据项）：读**数据项自己**的 `selector`，命中 `index` 中对应模板即用；未命中回退 `default`。

本质上是一种**判别联合（discriminated union）式的列表模型**：数据项携带 selector 去匹配模板表，而非所有项共用同一模板——形式是"按数据项内容分派渲染形态"。

### 流式合并（增量更新）

三种合并策略用于**局部化路径更新**，而非整体重绘：

- **Replace**：覆盖值。
- **Concat**：追加（文本拼接、数组 push、对象合并）。
- **Delete**：移除（文本替换、键移除）。

**关键**：合并是相对于**组件绑定的数据源**，组件通过 `id` 匹配合并。多个服务可流式传输到同一页面而互不冲突——这与 flux-architecture 的"命令增量 + 前端按 id 重放"是同一个原则，只是这里的"指令"描述的是 UI 结构而非业务数据。

## 组件系统设计权衡

### 封闭组件集，刻意不允许扩展

组件集是**有限封闭**的。三个约束共同决定：

1. **AI 可靠性** — 组件集是 AI 生成 JSON 的"词汇表"。词汇表无限膨胀，AI 无法可靠输出。封闭集让生产者（AI）可以穷举学习。
2. **WASM/包体积** — 每个组件增加加载体积。
3. **枚举即门卫** — 枚举是封闭集的显式边界，加组件必须手动过枚举这一关；这个摩擦力是故意的（禁用组件 = 从枚举删一行）。

边界可调整（组件替换），但总数不显著增长。

### 为什么枚举而非运行时注册

`inventory`/`linkme` 运行时注册（每个组件自注册、中央 registry 按名查找）不适用：

1. **WASM 兼容性** — 依赖链接器行为收集注册项，WASM 目标下可能静默丢失。
2. **类型擦除** — 运行时注册需统一成函数指针签名，编译期类型安全被破坏。
3. **编译期安全丢失** — 组件签名错、漏注册，运行时才报错。

### 为什么不用宏 DSL 替代枚举

1. **集中审计** — 枚举是组件一览表；宏 DSL 把定义分散到调用语法，失去集中管控。
2. **模块排除** — 禁用组件 = 从枚举删一行；宏方案需要额外机制控制。

### 开放组件系统在 Rust/WASM 下不可行

开放方案（inventory、typetag trait object、宏 DSL）在 Rust/WASM 下各有结构性问题。Rust 用编译期确定一切，WASM 下开放注册的基础设施不可靠。**封闭枚举不是妥协，是约束下的自然解。**（JavaScript 能开放是因为运行时动态性。）

## 状态：UI Store 与动作

- 持有 `layout`（当前 UI 树）、`data`（按 id 的数据）、`list`（按 id 的列表）三部分信号。
- 收到动作消息（Create、Set、Join、Tmpl）→ 更新对应状态。
- **流式合并**：`Join` 动作用合并策略（Replace/Concat/Delete），基于 `id` 匹配局部更新——只动被操作的节点，保留其余（折叠、局部展开等各实例状态独立）。

## 渲染

- **流式缓冲**：累积流式文本用分片缓冲（而非预连接或生命周期引用），每片 O(1) 摊销追加，信封可提前丢弃。
- **组件原子性**：直接映射标准 HTML/SVG 元素（Table、SVG），无过度语义包装——LLM 在标准元素上大量训练，保持原子确保生成准确性。
- **同步模板引擎**：轻量文本渲染是纯计算、无 IO、微秒级，无需异步卸载（异步包装反而增开销）。

## 设计时 vs 运行时

| 模式 | 触发 | 特性 |
|------|------|------|
| **设计时（AI 辅助）** | AI 生成 Brick JSON + 模拟数据，开发者预览后存为模板 | 降低编写组件门槛 |
| **运行时（模板驱动）** | 业务返回纯数据 → 应用模板 → 毫秒级渲染 | 零 LLM 延迟、确定性、无敏感数据进 LLM |
| **探索性（AI 生成）** | 结构不可预测时（仪表板、演示）AI 生成一次性 UI | 仅给极端场景 |

## 与 flux-architecture 的关系

- flux-architecture 描述**一般性原则**：数据如何流、如何推送、客户端如何按需字典增量消费。
- Flex UI 是**同一原则用于 UI 本体**——传的不再是业务数据，而是 UI 结构本身（Brick），且同样用"命令增量 + 按需（组件树作为 id-indexed 数据）"。
- 换句话说：**Flex UI 证明了 flux-architecture 的消费侧原则（增量指令流、按需字典、惰性加载）不是"传输数据的方案"，而是"传输任何递归结构体"的通用范式**——UI 树只是它的一种载荷。

## 交叉引用

- **[flux-architecture.md](flux-architecture.md)**：一般性的数据流动与客户端消费侧原则（本框架的传输基础）。
- **[projects/fluxora-architecture.md](projects/fluxora-architecture.md)**：Flex UI 的一个具体实现（Rust/Dioxus 事件驱动 UI 总线，含项目结构、编解码器、后端事件溯源等实现细节）。