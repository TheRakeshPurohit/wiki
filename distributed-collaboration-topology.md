# 分布式协作架构：拓扑与一致性的正交选择

**Status:** 初始版本
**核心论点：** 拓扑/信任边界（谁拥有数据、数据经过谁）与冲突解决算法（变更如何对齐）是两个正交维度，架构选型是二者的交叉组合而非单一光谱；跨所有权边界的海量数据走「大节点联邦 + 柔性事务」（联邦节点间 well-known 协议认证，元数据每节点独立单写，无共识），个人设备主权场景走本地优先（CRDT）；强共识（Raft/Paxos）保留给数据级强一致复制（外部 TiKV 类方案）。
**架构上下文：** [共识协议](consensus-protocol.md) — Raft 的机制与边界；[Aura 架构](aura-architecture.md) — 元数据单写、数据面本地的引擎落地

---

## 1. 两个正交维度

分析分布式协作系统，先剥离两个经常被混为一谈的维度：

- **维度 A：拓扑与信任边界**——身份归谁、数据所有权归谁、数据在物理上经过哪些节点。这是所有权与信任问题，协议解决不了。
- **维度 B：冲突裁决算法**——多个节点同时变更时，机器按什么规则让账目对齐。这是算法问题，不关心数据属于谁。

传统论述把「分布式」当作一条从单机到去中心化的单一光谱，实际是这两个维度的独立取值在交叉组合。按组合形态归纳为五类模型：

| 模型 | 维度 A：身份与所有权 | 拓扑 | 维度 B：一致性算法 | 代表系统 |
|:--|:--|:--|:--|:--|
| 垂直扩展单体 | 单一组织绝对中心 | 巨型单节点 | 单机并发锁 / ACID（MVCC，无跨网共识） | 早期大型机、单机 Oracle/DB2 |
| 集中式强共识 | 统一信任域（防宕机，不防背叛） | 同城/局域网对等节点 | Raft / Paxos / 2PC（Leader 独裁，全网单一视图） | etcd、ZooKeeper、TiDB-PD、Spanner |
| 大节点联邦 | 组织级分布式托管，节点互不信任、彼此对等，身份绑定所属节点 | 星型联邦网格，每节点自身是重型单体 | 柔性事务 / 最终一致（TCC / Saga / 异步对账，多重局部视图） | Email/SMTP、Matrix、ActivityPub |
| 去中心化中继 | 用户绝对主权（公钥即 ID，服务器不可封禁） | 哑管道：海量中继互不通信 | 客户端多播与去重（客户端拉取碎片、排序、验证） | Nostr |
| 本地优先 | 个人设备最高主权，数据先属本地，网络仅同步通道 | 纯边缘 P2P（LAN/蓝牙/DHT，无服务器） | CRDT（单调格理论，乱序输入，数学自收敛） | Figma 底层、Automerge、Yjs、Anytype |

选型时先回答维度 A——数据的所有权与信任边界是业务事实，不是技术偏好；维度 B 的算法再在边界内优化。倒过来（先选算法再塞所有权）会得到「用 Raft 统一一切」或「用 CRDT 做账务」这类范畴错误。

## 2. 强共识的本质与物理天花板

### 2.1 模拟单机，不是扩展

Raft/Paxos 的工程目的不是扩容、也不是去中心化——是用多机冗余掩盖单机故障，在应用层模拟一个绝对可靠的「单机体验」（线性一致）。组内 Leader 独裁写编排，Follower 无决策主权，存在意义是选举候选与只读副本。这与 [共识协议](consensus-protocol.md) 的定位一致：**Raft 是元数据共识协议，不是数据存储协议**。

### 2.2 广域网是物理天花板

写必须等多数派落盘才能返回成功，这一条在广域网上被物理延迟锁死：

- 跨城 RTT 30-40ms（北京-上海-广州三地集群），高并发下每次事务锁持有数十毫秒 → 连接池耗尽、线程阻塞、吞吐断崖。
- 写放大随节点数线性增长（3 节点 3x 存储 + 2x 网络传输）——详见 [共识协议 §写放大](consensus-protocol.md#raft-不适合什么批量数据存储)。

这不是工程优化能消除的——是光速与多数派数学的乘积。能消除它的是架构选择：不把跨广域的数据放进同一个共识组。

### 2.3 存储引擎是第二重瓶颈

etcd 底层是纯 Go 的单机 MVCC KV（BoltDB），默认配额硬性锁在 2-8GB，超过后即使 Compact/Defrag 也因 Raft 日志单条有序性而性能雪崩。单机嵌入式引擎（RocksDB 系 LSM-Tree）在纯垂直方向上的写 IOPS 与容量上限高出 1-2 个数量级——「共识层的存储瓶颈」与「共识协议本身的成本」是两个独立问题，都指向同一结论：强共识组保持小数据量。

## 3. 大节点联邦：对内单体红利，对外对等联邦

### 3.1 单体垂直红利是被算法复杂性掩盖的选项

现代顶配单机（128 核/256 线程、TB 级 ECC 内存、PCIe 5.0 NVMe 数百万 IOPS）的物理吞吐，足以碾压一个忙于旗语投票的弱节点 Raft 集群。省市级的日常核心写峰值（医保、政务、清算）通常每秒几千到几万笔——单机或同城双活的重型单体直接吃下，读写微秒级。

用 Raft 做区域内水平扩展，是「以极高算法复杂度换取微弱收益」的典型错配。区域内该做的是：**堆硬件 + 堆软件效率，消解算法复杂度**。

### 3.2 疆域内部：两种形态

- **极致单体**：单机顶配 + 高性能数据库（多核优化 PostgreSQL / 嵌入式 RocksDB 变种），本机 RAM/NVMe 落盘即成功，微秒级延迟。
- **同城高可用**：同城 <1ms 网络内 3 节点 Raft 组，只防机房级不可抗力，对外完全屏蔽共识细节。这是 [共识协议](consensus-protocol.md) 中 Raft 的正确领地：小数据量、同城 RTT 下多数派确认代价可忽略。

### 3.3 疆域之间：对等外交

跨区域节点地位绝对平等：不共享共识日志、不选全局 Leader、不追求单一视图。跨区域业务（A 省用户在 B 省就医报销）走联邦标准接口，各自维护独立本地视图，事后对账。信任边界在此处发挥作用——节点互不信任，所以身份绑定所属节点、能力声明走注册表，而非共享一个权限中心。

这正是 [Aura 架构](aura-architecture.md) 的拓扑判断在组织尺度上的映射：场域内 Actor 状态本地写（不走 Raft），元数据才走共识；Probe 出站注册、用户 namespace 隔离——同一套「控制面小而强、数据面本地而快」的分解。

## 4. 联邦柔性事务：TCC 与两个自愈补丁

放弃全网强共识后，跨联邦业务无法用分布式锁锁两边数据——资金双花风险由业务层两阶段柔性事务（TCC）承担。注意这与 [分布式锁反模式](distributed-lock-anti-pattern.md) 的判据一致：TCC 处理的是**协调与补偿**，不是互斥；Try/Confirm/Cancel 是状态机，不是锁。

### 4.1 三阶段状态机

以跨联邦转账 100 元为例：

```
[ A 联邦（北京）]                                [ B 联邦（广州）]
      |                                              |
      |-- (1) Try: 本地冻结 100 元 ----------------->|
      |   （冻结资金表，钱未离账但不可用）             |-- 检查账户合法性（冻结/销户）
      |<---------------- Try-OK（B 本地挂账预留）-----|
      |                                              |
      |-- (2) Confirm: 本地正式扣款 ----------------->|  释放预留，入账 +100
      |   （异步重试直至确认）                         |
      |                                              |
      | [ Try 失败/超时 ]                             |
      |-- (3) Cancel: 本地解冻 --------------------->|  取消预留
```

- **Try**：A 本地冻结（A 视图内不可能双花），经 mTLS/WireGuard 上的 gRPC 向 B 发预检与额度预留；B 校验收款方状态，本地写挂账预留，返回 Try-OK。
- **Confirm**：A 收到双边成功确认后本地落不可逆流水，异步通知 B；因 Try 已完成严格预检，Confirm 逻辑上必成——网络抖动不需要回滚，靠本地可靠消息（Outbox 模式重试队列）重试到 B 确认为止。
- **Cancel**：B 返回不可收款或超时，A 本地解冻，用户账单上只有一次「转账未发生」报错，没有「先扣再冲」的脏账目。

用户体验要点在 Try 的设计：**资金冻结而非扣减**，失败即无痕——补偿式回滚（Saga）做不到这一点，Saga 会留下正反两条流水。

### 4.2 两个自愈补丁

**幂等去重（Idempotency）**：B 已入账但回执在途中丢失，A 的 Outbox 重试会重发 Confirm。补丁：Try 阶段由 A 生成全局唯一 XID（时间戳 + 节点 ID + 序列），B 本地建以 XID 为唯一索引的去重状态机表——重复 XID 触发唯一键冲突，B 直接返回「已成功」，绝不重复加钱。这是 [分布式锁反模式 §1](distributed-lock-anti-pattern.md#1-术语澄清哪些不是锁) 判据的正面应用：**幂等/去重靠幂等键 + 唯一约束，不是锁**。

**悬挂回查（TTL）**：A 在发送 Confirm 前整体毁灭，B 的预留被永久锁死。补丁：B 的预留表内嵌 TTL（如 5 分钟）；超时后守护线程不盲目取消，而是向 A 预设的异地灾备节点反向回查该 XID 的终态——回复未完成则 Cancel 释放预留，回复已 Confirm 则本地强行推进到 Confirm 完成入账。**悬挂的解法是回查终态，不是超时自动回滚**——后者在「实际已 Confirm」的场景下造成资金凭空消失。

两个补丁共同的哲学：每一方只依赖**本地状态 + 对端可查询的声明**，不依赖跨联邦的实时互斥。这和 [分布式锁反模式](distributed-lock-anti-pattern.md) 的核心论点同构——用协调（状态机 + 幂等 + 回查）替代阻塞（锁）。

## 5. 控制面与数据面的收敛

跨所有权边界的数据面不共享存储、不共享共识日志（元数据每节点独立，控制平面单写）。联邦节点之间的信任建立走 **well-known 协议认证**：身份经公钥/证书互认，数据主权绑定所属节点——用户登录其它节点，该节点没有它的历史数据，不存在全局登录同步。

```
┌─────────────────────────────────────────────────────────┐
│              联邦节点（各自独立）                          │
│  本地：单体数据面 + meta 实例（控制平面单写，无共识）          │
│  之间：well-known 协议认证 + 柔性事务（TCC/Saga 对账）       │
└─────────────────────────────────────────────────────────┘
```

- **对内**：所有权边界内的业务数据用单体垂直红利消解算法复杂度；元数据由控制平面单写、节点缓存，不需要共识——单一逻辑写入者使多数派投票失去对象。
- **对外**：跨所有权边界用 TCC/Saga 拥抱多重视图、最终对齐；身份信任经 well-known 协议建立。

这条分界线是 [共识协议](consensus-protocol.md) 核心论点的延伸：**Raft 的领地 = 小容量 + 强一致**——但元数据在「控制平面单写」下连这个小领地也不需要共识；Raft/Paxos 保留给数据级强一致复制（TiKV 类）与真正的多写者元数据（独立目录服务）。数据面的一致性方案由维度 A（信任边界）决定——边界内单机事务，跨边界柔性事务，个人主权场景 CRDT。

## 6. 本地优先的位置

第五类模型（CRDT 本地优先）与前四类的分界不在规模，在**数据主权的归属**：个人生产力与协同编辑场景里，设备本身是权威节点，网络只是同步通道。CRDT 以单调格的数学性质做到乱序合并、无需中心仲裁——代价是数据模型必须表达为可合并的格（计数器、集合、序列），任意业务约束（如余额不为负）无法放进 CRDT 里。

因此它与联邦不是竞争关系，是维度 A 的另一个取值：数据属于个人设备 → CRDT；数据属于组织 → 联邦；元数据属于系统 → Raft。三者按数据类别共存于同一系统（如协同文档走 CRDT、用户账户走联邦、服务目录走 Raft）。

## 7. 液态组织的协议层：Nostr 错配与 well-known 冷引导

AI 时代的组织形态是液态的——超级个体按项目临时聚合、完成后解散（见 [AI 时代的组织和个人](ai-era-organization-individual.md) 联邦化一节）。经济层（代币结算、投票定价、AI 主持人协调）之外，还有一个必须回答的问题：**这些临时组织在物理上怎么组网**。协议选型由液态组织的一对矛盾决定：

- **对内要液态**：成员高频进出，不能有沉重的审批流。
- **对外必须有边界**：知识库、账本、未公开的成果，不能像公共广场一样敞开。

### Nostr 的错配

Nostr 类纯公共中继协议是维度 A 取「公共、无主权」的解：所有事件在公开中继流转，任何客户端可拉取。这天然适配公共社交，天然不适配有边界的生产组织——即使内容加密，**元数据仍然泄露**（谁在和谁协作、协作频率、数据量级）。纯公共中继承载有边界组织是范畴错配，不是加密强度问题。

### well-known 冷引导 + 联邦热通道

成立的分层是：**发现走静态分发，协同走联邦连接**——分层成本原则的协议层投影（便宜通道做大范围，贵通道只服务收敛后的连接）：

```
阶段一 · 初始化   发起者在自有域名发布 /.well-known/<org>.json：
                  成员公钥清单 + 端点，私钥签名 → 组织的第一道虚拟边界
阶段二 · 发现     参与者拉取该文件，同步全量成员通讯录——
                  静态 HTTP 分发解决 P2P 最难的引导问题（怎么找到对方）
阶段三 · 协同     成员间按清单建立联邦连接，高频数据在边界内流动，
                  外界只见一个域名
阶段四 · 流动     成员进出 = 更新清单 → 各节点重新拉取 → 连接重组
```

这个分层的本质：**用 Web2 最成熟的静态分发技术给 Web3 身份（公私钥）做寻址**。Nostr 的中继在此没有位置——中继承担的「发现」职能被一个静态文件以近零成本替代，而中继承担的「消息流转」职能恰恰是液态组织要拒绝的。

### 冷引导与成员列表的两分

诚实标注这套方案的边界：**冷引导（首次找到彼此）与成员列表演进是两个问题**。

- 冷引导是低频一次性操作，静态 well-known 文件完美胜任——发起者单点恰好就是权威源。
- 成员列表是**有状态数据**：组织生命周期超过一次会话后，它需要版本化（并发更新的仲裁）、撤销（退出成员的凭证失效）、密钥轮换。静态 JSON 不承载这些——发起者宕机时发现机制随之死亡，「多签名公告栏」的补丁加上去，本质就是重新发明中继。

所以诚实的结论不是「well-known 完美闭环」，而是：**well-known 只负责冷引导；成员列表作为长期资产，归宿是某种有状态的注册表形态**——它长什么样（单写者、多签名、还是轻量共识）是开放问题，取决于液态组织的规模与寿命。协议选型与组织形态的对应：**短命组织用静态清单够用，长命组织必然长出注册表**。

### 与五模型的关系

液态组织在维度 A 上是「临时有界主权」——比联邦轻（生命周期按项目计），比个人主权重（有明确的边界和成员名单）。协议组合也相应是混合的：well-known 静态清单定边界，成员间联邦式互信，协同编辑走 CRDT。五模型不是互斥选项，是按数据类别与组织寿命拼装的调色盘。

## 交叉引用

- **[共识协议](consensus-protocol.md)**：Raft 机制细节、写放大账目、etcd 案例、何时用/不用 Raft 的决策矩阵——本文控制面结论的机制基础。
- **[AI 时代的组织和个人](ai-era-organization-individual.md)**：液态组织（联邦化 OPC）的经济层——代币结算、投票定价、AI 主持人协调；本文 §7 补其协议层。
- **[分布式锁反模式](distributed-lock-anti-pattern.md)**：TCC/幂等/回查是协调不是互斥的判据来源；「锁掩盖协调缺失」在联邦事务中的正面展开。
- **[Aura 架构](aura-architecture.md)**：「控制面小而强、数据面本地而快」的引擎级落地——Actor 状态本地写、元数据走共识、用户 namespace 隔离。
- **[KV 存储引擎](kv-storage-engine.md)**：共识与协调层级的存储选型（Fjall/SlateDB 双引擎）。
- **[Redis 批判](redis-critique.md)**：跨联邦缓存与锁为什么交给 KV 与共识层。
